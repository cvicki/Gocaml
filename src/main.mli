(** Main is the terminal-based interface for GOcaml *)

(** [main] launches the terminal-based version for GOcaml, prompting the user 
    for a variety of relevant inputs. *)
val main : unit -> unit
